# ==================================================================
# Scarlet feature flags for the "minimal" edition.
# Keeps the core VRChat audit-log + Discord/TTS pipeline while cutting
# optional subsystems that are not part of the barebones build.
# ==================================================================

rvc.enabled=false
discord.kick_ban.enabled=false
watched.avatars.enabled=false
calendar.enabled=false
aux_webhooks.enabled=false
animated_emoji.enabled=false
dave.enabled=false
evidence.enabled=false
avatar_search.enabled=false
pronouns.enabled=false
vrchat_reports.enabled=false
cli_commands.enabled=false
