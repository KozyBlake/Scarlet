<?xml version="1.0" encoding="utf-8"?>
<!--
  Scarlet Android manifest.

  All of the privileged work (reading VRChat's output_log files from inside
  its sandbox) is performed through adb shell commands over the loopback
  ADB-over-TLS socket the user authorized once via Wireless Debugging
  pairing.  No MANAGE_* storage permission is requested, no PC is needed
  at any point, and no ContentProvider is exported - everything flows
  back into desktop Scarlet through the mirrored log file Scarlet writes
  to its own internal storage.
  -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="net.sybyline.scarlet.android">

    <uses-sdk
        android:minSdkVersion="30"
        android:targetSdkVersion="34" />

    <!-- libadb-android opens a TLS socket against the loopback adbd that
         Wireless Debugging exposes - INTERNET is required even for the
         loopback path because Android treats java.net.Socket as a network
         operation regardless of host. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- NSD scans for Wireless Debugging's _adb-tls-pairing._tcp /
         _adb-tls-connect._tcp service advertisements. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Foreground service to keep the ADB socket + file tail alive while
         the user is inside VRChat.  Android 14+ requires an explicit type. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Exempt Scarlet from doze + OEM-specific background killers (Samsung
         Freecess, MIUI auto-launch, OxygenOS app sleep, etc.) so the ADB
         socket and tail loop survive screen-off / app-backgrounded.  Without
         this, Samsung freezes the entire UID after ~10 seconds of being
         idle and the file tail stops mid-stream. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- PARTIAL_WAKE_LOCK held by ScarletLogService for the duration of
         the supervisor loop so the CPU doesn't sleep on us while we tail
         VRChat's log.  Foreground service alone doesn't keep the CPU
         awake; we need an explicit wake lock for that. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/Theme.Scarlet"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ScarletLogService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
